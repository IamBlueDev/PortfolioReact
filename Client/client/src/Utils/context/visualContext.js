//This file contains visual dataset context

import React from 'react';

export default React.createContext({
    colorSchme: "dull",
    windowHeight:"0",
    windowWidth:"0",
    mode:"desktop",
})